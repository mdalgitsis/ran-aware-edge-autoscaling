apiVersion: edge.ranaware.dev/v1
kind: EdgeAppPlacement
metadata:
  name: sample-placement
  namespace: default
spec:
  appId: "edge-app-001"
  edgeNodeId: "edge-node-42"
  kafka:
    broker: "kafka:9092"
    topic: "paths"
    message:
      eventType: "MIGRATION"

