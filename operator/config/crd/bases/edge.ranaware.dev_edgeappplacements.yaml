---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: edgeappplacements.edge.ranaware.dev
spec:
  group: edge.ranaware.dev
  names:
    kind: EdgeAppPlacement
    listKind: EdgeAppPlacementList
    plural: edgeappplacements
    singular: edgeappplacement
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: EdgeAppPlacement is the Schema for the edgeappplacements API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EdgeAppPlacementSpec defines the desired state of EdgeAppPlacement.
            properties:
              appId:
                description: AppID identifies the application whose placement is being
                  tracked.
                type: string
              edgeNodeId:
                type: string
              kafka:
                properties:
                  broker:
                    type: string
                  message:
                    properties:
                      eventType:
                        type: string
                    required:
                    - eventType
                    type: object
                  topic:
                    type: string
                required:
                - broker
                - message
                - topic
                type: object
            required:
            - appId
            - edgeNodeId
            - kafka
            type: object
          status:
            description: EdgeAppPlacementStatus defines the observed state of EdgeAppPlacement.
            properties:
              kafkaNotificationSent:
                type: boolean
              lastNotificationTime:
                type: string
              lastNotifiedEdgeNodeId:
                description: |-
                  LastNotifiedEdgeNodeID is the edge node the last notification announced.
                  Comparing it against Spec.EdgeNodeID is what makes a migration detectable
                  and the notification exactly-once.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
